validation.body.flat.createCode.notnull=body.create_code is required
validation.body.flat.createCode.empty=body.create_code can't be empty

validation.body.flat.name.notnull=body.name is required
validation.body.flat.name.empty=body.name can't be empty
validation.body.flat.name.regex=body.name must match the pattern '^[a-z-]+$'
